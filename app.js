const usernameInput = document.querySelector('.username input');
const passwordInput = document.querySelector('.password input');
const submit = document.querySelector('.submit button');

//make function to check usernameInput, send alert if reqs not met, send alert if met, clear input
function checkUsername() {

}

//make function to check passwordInput, send alert if reqs not met, send alert if met, clear input
function checkPassword() {
    
}

//wrap both functions inside one function, which will be called when button is clicked
function checkInputs() {

}

submit.addEventListener('click', checkInputs);